diff --git a/apps/api/src/modules/dg-circuit/dg-review.service.ts b/apps/api/src/modules/dg-circuit/dg-review.service.ts
index f8fb3ae..7a1add3 100644
--- a/apps/api/src/modules/dg-circuit/dg-review.service.ts
+++ b/apps/api/src/modules/dg-circuit/dg-review.service.ts
@@ -23,39 +23,44 @@ export const createDgReview = async (params: {
   sentToDgAt?: Date;
   observations?: string;
 }): Promise<{ _id: Types.ObjectId }> => {
-  const existingReview = await DGReviewModel.findOne({
+  // A target (request, formal request courrier, etc.) can go through more than
+  // one DG circuit cycle over its lifetime - e.g. an initial courrier rejected
+  // by the DG is corrected and resent. Each cycle gets its own DGReview
+  // document so a prior rejected/closed decision remains an immutable
+  // historical record. Only a genuinely active (non-terminal) review for the
+  // same target blocks a new one - that is the actual protection this guard
+  // should provide (no duplicate concurrent circuits), not "only ever one
+  // review per target".
+  const latestReview = await DGReviewModel.findOne({
     targetId: params.targetId,
     targetType: params.targetType,
-  }).lean();
+  })
+    .sort({ createdAt: -1 })
+    .lean();
 
   if (
-    existingReview?.status &&
-    DG_REVIEW_TERMINAL_STATUSES.includes(
-      existingReview.status as (typeof DG_REVIEW_TERMINAL_STATUSES)[number],
+    latestReview?.status &&
+    !DG_REVIEW_TERMINAL_STATUSES.includes(
+      latestReview.status as (typeof DG_REVIEW_TERMINAL_STATUSES)[number],
     )
   ) {
-    throw new HttpError(409, "Le circuit DG est deja cloture pour cet element.");
+    throw new HttpError(409, "Un circuit DG est deja en cours pour cet element.");
   }
 
-  const review = await DGReviewModel.findOneAndUpdate(
-    { targetId: params.targetId, targetType: params.targetType },
-    {
-      $set: {
-        targetType: params.targetType,
-        targetId: params.targetId,
-        requestId: params.requestId,
-        dossierId: params.dossierId,
-        phaseId: params.phaseId,
-        status: "awaiting_return",
-        handledByRole: toDgRole(params.handledByRole),
-        handledById: params.handledById,
-        sentToDgAt: params.sentToDgAt ?? new Date(),
-        outgoingDocumentId: params.outgoingDocumentId,
-        observations: params.observations,
-      },
-    },
-    { new: true, upsert: true, setDefaultsOnInsert: true },
-  );
+  const review = await DGReviewModel.create({
+    targetType: params.targetType,
+    targetId: params.targetId,
+    requestId: params.requestId,
+    dossierId: params.dossierId,
+    phaseId: params.phaseId,
+    status: "awaiting_return",
+    handledByRole: toDgRole(params.handledByRole),
+    handledById: params.handledById,
+    sentToDgAt: params.sentToDgAt ?? new Date(),
+    outgoingDocumentId: params.outgoingDocumentId,
+    observations: params.observations,
+  });
+
   return { _id: review._id };
 };
 
diff --git a/apps/api/src/modules/requests/constants/request.constants.ts b/apps/api/src/modules/requests/constants/request.constants.ts
index d617d84..f962049 100644
--- a/apps/api/src/modules/requests/constants/request.constants.ts
+++ b/apps/api/src/modules/requests/constants/request.constants.ts
@@ -26,6 +26,8 @@ export const EDITABLE_STATUSES = [
   "draft",
   "courrier_uploaded",
   "courrier_physical_declared",
+  "intake_requires_correction",
+  "initial_dg_decision_recorded",
 ] as const;
 
 export const INTAKE_MUTABLE_STATUSES = [
diff --git a/apps/portal/src/pages/PortalDashboardPage.tsx b/apps/portal/src/pages/PortalDashboardPage.tsx
index e314a42..7e4c059 100644
--- a/apps/portal/src/pages/PortalDashboardPage.tsx
+++ b/apps/portal/src/pages/PortalDashboardPage.tsx
@@ -34,7 +34,10 @@ function deriveStats(
   return {
     demandes: requests.filter((r) => r.status !== "draft").length,
     actionsRequises: requests.filter(
-      (r) => r.actionRequired || r.status === "intake_requires_correction",
+      (r) =>
+        r.actionRequired ||
+        r.status === "intake_requires_correction" ||
+        r.status === "initial_dg_decision_recorded",
     ).length,
     enCours: requests.filter((r) => r.status === "dossier_opened").length,
     notifications: unreadCount,
diff --git a/apps/portal/src/pages/RequestDetailPage.tsx b/apps/portal/src/pages/RequestDetailPage.tsx
index 1a58b17..37b742c 100644
--- a/apps/portal/src/pages/RequestDetailPage.tsx
+++ b/apps/portal/src/pages/RequestDetailPage.tsx
@@ -96,10 +96,15 @@ export function RequestDetailPage(): React.JSX.Element {
   const reqUploadFileRef = useRef<HTMLInputElement>(null);
 
   const request = detail?.request;
+  // Mirrors the backend's EDITABLE_STATUSES (apps/api .../request.constants.ts):
+  // a request stays editable while in draft, and becomes editable again when
+  // a correction or a DG rejection sends it back for resubmission.
   const isSubmitted = request
     ? request.status !== "draft" &&
       request.status !== "courrier_uploaded" &&
-      request.status !== "courrier_physical_declared"
+      request.status !== "courrier_physical_declared" &&
+      request.status !== "intake_requires_correction" &&
+      request.status !== "initial_dg_decision_recorded"
     : false;
 
   const hasFormalDocRequired =
diff --git a/apps/portal/src/pages/my-requests/RequestCardGrid.tsx b/apps/portal/src/pages/my-requests/RequestCardGrid.tsx
index 593054b..94075bf 100644
--- a/apps/portal/src/pages/my-requests/RequestCardGrid.tsx
+++ b/apps/portal/src/pages/my-requests/RequestCardGrid.tsx
@@ -22,7 +22,9 @@ export function RequestCardGrid({
   return (
     <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">
       {requests.map((request) => {
-        const hasAction = request.status === "intake_requires_correction";
+        const hasAction =
+          request.status === "intake_requires_correction" ||
+          request.status === "initial_dg_decision_recorded";
 
         return (
           <button
diff --git a/apps/portal/src/pages/my-requests/RequestTableList.tsx b/apps/portal/src/pages/my-requests/RequestTableList.tsx
index cd1c0db..059e4c4 100644
--- a/apps/portal/src/pages/my-requests/RequestTableList.tsx
+++ b/apps/portal/src/pages/my-requests/RequestTableList.tsx
@@ -24,7 +24,9 @@ export function RequestTableList({
 
       <div className="divide-y divide-slate-100">
         {requests.map((request) => {
-          const hasAction = request.status === "intake_requires_correction";
+          const hasAction =
+            request.status === "intake_requires_correction" ||
+            request.status === "initial_dg_decision_recorded";
 
           return (
             <button
diff --git a/apps/portal/src/pages/request-detail/RequestActionsTab.tsx b/apps/portal/src/pages/request-detail/RequestActionsTab.tsx
index 70c13e6..b06cdc5 100644
--- a/apps/portal/src/pages/request-detail/RequestActionsTab.tsx
+++ b/apps/portal/src/pages/request-detail/RequestActionsTab.tsx
@@ -93,7 +93,8 @@ export function RequestActionsTab({
       </Card>
 
       <div className="flex flex-col gap-4">
-        {request.status === "intake_requires_correction" ? (
+        {request.status === "intake_requires_correction" ||
+        request.status === "initial_dg_decision_recorded" ? (
           <Alert variant="warning" appearance="light">
             <AlertIcon>
               <AlertCircle
@@ -105,9 +106,8 @@ export function RequestActionsTab({
             <AlertContent>
               <AlertTitle>Correction requise</AlertTitle>
               <p className="mt-1 text-sm text-amber-700">
-                Votre demande nécessite une correction. Veuillez contacter
-                l'équipe ANAC ou soumettre un dossier corrigé selon les
-                instructions reçues.
+                Votre demande nécessite une correction. Consultez l'onglet
+                Courrier pour renvoyer un courrier corrigé.
               </p>
             </AlertContent>
           </Alert>
diff --git a/apps/portal/src/pages/request-detail/status.constants.ts b/apps/portal/src/pages/request-detail/status.constants.ts
index a8aea02..67ab6b1 100644
--- a/apps/portal/src/pages/request-detail/status.constants.ts
+++ b/apps/portal/src/pages/request-detail/status.constants.ts
@@ -13,7 +13,7 @@ export const REQUEST_STATUS_LABELS: Record<PortalRequestStatus, string> = {
   intake_requires_correction: "Action requise",
   initial_sent_to_dg: "En attente d'orientation administrative",
   initial_dg_returned: "En cours de traitement administratif",
-  initial_dg_decision_recorded: "En cours de traitement administratif",
+  initial_dg_decision_recorded: "Action requise",
   oriented_to_dn: "Transmise à la Direction de la Navigabilité",
   rejected: "Demande annulée",
   reoriented: "En cours de traitement administratif",
@@ -105,4 +105,7 @@ export const PRELIMINARY_PHASE_STATUS = new Set(["dossier_opened"]);
 /**
  * Request status values that require immediate action
  */
-export const ACTION_REQUIRED_STATUSES = new Set(["intake_requires_correction"]);
+export const ACTION_REQUIRED_STATUSES = new Set([
+  "intake_requires_correction",
+  "initial_dg_decision_recorded",
+]);
diff --git a/apps/portal/src/pages/request-detail/status.helpers.ts b/apps/portal/src/pages/request-detail/status.helpers.ts
index 3ef7ed9..d0be847 100644
--- a/apps/portal/src/pages/request-detail/status.helpers.ts
+++ b/apps/portal/src/pages/request-detail/status.helpers.ts
@@ -135,6 +135,11 @@ export function getActionMessage(
     return "Action requise: Corrections demandées";
   }
 
+  // Initial courrier rejected by the DG, resubmission required
+  if (request.status === "initial_dg_decision_recorded") {
+    return "Action requise: Courrier initial rejeté, correction attendue";
+  }
+
   // Preliminary form available
   if (
     dossier?.preliminary?.status === "pre_eval_form_available" &&
